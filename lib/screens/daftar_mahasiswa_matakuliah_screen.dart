import 'package:flutter/material.dart';

class DaftarMahasiswaMatakuliahScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Daftar Mahasiswa dan Matakuliah')),
      body: const Center(
        child:
            Text('Daftar Mahasiswa dan Matakuliah akan ditampilkan di sini.'),
      ),
    );
  }
}
