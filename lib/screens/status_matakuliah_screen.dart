import 'package:flutter/material.dart';

class StatusMatakuliahScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Status Matakuliah')),
      body: const Center(
        child: Text('Form Status Matakuliah akan ditampilkan di sini.'),
      ),
    );
  }
}
